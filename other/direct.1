;; -*-lisp-*-
(comment list 40000.)

(let ((^w t) ;;shut up the printer
      (old-crunit (crunit)))
     (load '|ai:ken;ken lisp|)
     (nouuo nil)
     (load '|ai:ken;load|)
     (director '(direct))
     (apply 'crunit old-crunit)
     '|Welcome to Director|)


